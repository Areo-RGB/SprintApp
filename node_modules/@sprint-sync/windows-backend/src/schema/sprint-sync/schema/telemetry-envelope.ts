// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TelemetryPayload, unionToTelemetryPayload, unionListToTelemetryPayload } from '../../sprint-sync/schema/telemetry-payload.js';


export class TelemetryEnvelope {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TelemetryEnvelope {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTelemetryEnvelope(bb:flatbuffers.ByteBuffer, obj?:TelemetryEnvelope):TelemetryEnvelope {
  return (obj || new TelemetryEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTelemetryEnvelope(bb:flatbuffers.ByteBuffer, obj?:TelemetryEnvelope):TelemetryEnvelope {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TelemetryEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

payloadType():TelemetryPayload {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : TelemetryPayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startTelemetryEnvelope(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:TelemetryPayload) {
  builder.addFieldInt8(0, payloadType, TelemetryPayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, payloadOffset, 0);
}

static endTelemetryEnvelope(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishTelemetryEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedTelemetryEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createTelemetryEnvelope(builder:flatbuffers.Builder, payloadType:TelemetryPayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  TelemetryEnvelope.startTelemetryEnvelope(builder);
  TelemetryEnvelope.addPayloadType(builder, payloadType);
  TelemetryEnvelope.addPayload(builder, payloadOffset);
  return TelemetryEnvelope.endTelemetryEnvelope(builder);
}
}
